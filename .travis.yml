addons:
  apt:
    packages:
      - fakeroot
      - rpm
language: java
jdk: openjdk11
script: ./ganttproject-builder/build-travis.sh
branches:
  only:
    - master
    - /^tkt-.*$/
    - /^prj-.*$/
